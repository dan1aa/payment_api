{{#if isAuth}}
        {{#if isUserPay}}
                <a href="/apikey"><button class="apikey_link_button">GO TO API KEY</button></a>
                <span>Loggined as {{username}}</span>
        {{else}}
                <h1>Get your api now</h1>
                <form action="/pay" method="POST"><button class="getapibutton">GET API</button></form>
        {{/if}}
{{else}}
        <h1>Sorry, but to get api, you must authorize</h1>
{{/if}}


